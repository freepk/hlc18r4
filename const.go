package main

const (
	accBirthKey     = `"birth":`
	accBirthLen     = len(accBirthKey)
	accCityKey      = `"city":`
	accCityLen      = len(accCityKey)
	accCountryKey   = `"country":`
	accCountryLen   = len(accCountryKey)
	accEmailKey     = `"email":`
	accEmailLen     = len(accEmailKey)
	accIdKey        = `"id":`
	accIdLen        = len(accIdKey)
	accJoinedKey    = `"joined":`
	accJoinedLen    = len(accJoinedKey)
	accFnameKey     = `"fname":`
	accFnameLen     = len(accFnameKey)
	accInterestsKey = `"interests":`
	accInterestsLen = len(accInterestsKey)
	accLikesKey     = `"likes":`
	accLikesLen     = len(accLikesKey)
	accPhoneKey     = `"phone":`
	accPhoneLen     = len(accPhoneKey)
	accPremiumKey   = `"premium":`
	accPremiumLen   = len(accPremiumKey)
	accSexKey       = `"sex":`
	accSexLen       = len(accSexKey)
	accSnameKey     = `"sname":`
	accSnameLen     = len(accSnameKey)
	accStatusKey    = `"status":`
	accStatusLen    = len(accStatusKey)
)

const (
	likesIdKey = `"id":`
	likesIdLen = len(likesIdKey)
	likesTsKey = `"ts":`
	likesTsLen = len(likesTsKey)
)

const (
	premiumFinishKey = `"finish":`
	premiumFinishLen = len(premiumFinishKey)
	premiumStartKey  = `"start":`
	premiumStartLen  = len(premiumStartKey)
)
